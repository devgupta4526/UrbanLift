spring:
  application:
    name: Uber-DriverService

  datasource:
    url: jdbc:mysql://localhost:3306/uber_db_local
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        enable_lazy_load_no_trans: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true

jwt:
  expiry: 3600
  secret: undergroundjddkdjkdjododkdjdkldkdldjkdkdljdljdkldjldldjdldjldjldjdldljdldj
